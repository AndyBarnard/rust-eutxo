use crate::address;

//structure of network:
//is used for indexing addresses
//still not sure if I want to keep the network module, still sketching...

struct Network {
    addrs: Vec<Address>,
}

impl Network {
    fn new() -> Self {
        Self {

        }
    }
}